document.addEventListener('DOMContentLoaded',()=>{const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();const form=document.getElementById('interest-form');if(!form)return;form.addEventListener('submit',(event)=>{event.preventDefault();const name=document.getElementById('name').value.trim();const email=document.getElementById('email').value.trim();const reason=document.getElementById('reason').value;const message=document.getElementById('message').value.trim();const body=['Stride Dynamics Lab website inquiry','',`Name: ${name}`,`Email: ${email}`,`Reason: ${reason}`,'','Message:',message||'(No message provided)'].join('\n');const mailto=`mailto:matt@mwstrategicadvisors.com?subject=${encodeURIComponent('Stride Dynamics Lab inquiry')}&body=${encodeURIComponent(body)}`;window.location.href=mailto;});});
